{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "主窗：原生标题栏拖动/关窗，webview 侧只需核心默认权限",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}

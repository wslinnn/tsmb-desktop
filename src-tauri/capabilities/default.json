{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "lyrics"],
  "permissions": [
    "core:default",
    "opener:default",
    "store:default"
  ]
}

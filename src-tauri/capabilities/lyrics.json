{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "lyrics-capability",
  "description": "歌词窗：拖动歌词条 + 控制条唤回主窗；不需要 opener/store（那是 Rust 侧用的）",
  "windows": ["lyrics"],
  "permissions": [
    "core:default",
    "core:window:allow-show",
    "core:window:allow-set-focus",
    "core:window:allow-start-dragging"
  ]
}
